const {
  getItemsAll,
  getItemsById,
  getItemsSuggest,
  getFilteredItems,
  getFilteredItemsAdmin,
  getItemRecentlyAdded,
  getFeaturedItems
} = require('./actions');

module.exports = {
  getItemsAll,
  getItemsById,
  getItemsSuggest,
  getFilteredItems,
  getFilteredItemsAdmin,
  getItemRecentlyAdded,
  getFeaturedItems
};
